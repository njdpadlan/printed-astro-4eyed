---
import { Image } from 'astro:assets';
const {aboutHeader, aboutImage, aboutText, requestQuoteBtn, aboutAltText, contactUsBtn } = Astro.props;


---

<div>
    <h2>{aboutHeader}</h2>
        <Image 
            src={aboutImage} 
            alt={aboutAltText}
            inferSize={true}/>
    <p>{aboutText}</p>
    <a href="#">{requestQuoteBtn}</a>
    <a href="#">{contactUsBtn}</a>
</div>